# multi-task-distilbert
This implementation provides a scalable solution for binary classification of long textual documents that exceed transformer input length limits. It supports multiple independent classification tasks while maintaining parameter efficiency and clean task isolation.
